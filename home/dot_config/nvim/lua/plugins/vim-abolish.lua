-- Gives you the ability to find/replace multiple variants of a word at once,
-- which I use once a decade, but more importantly gives you coercion between
-- snake_case, camelCase, and everything else. I use this all the time.
return { { "tpope/vim-abolish" } }
